name: Email Step Example

on: [push]

jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
      - name: 22f3000710@ds.study.iitm.ac.in
        run: echo "This step is named exactly as the email."
